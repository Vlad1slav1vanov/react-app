import React from "react";
import Button from "./UI/Button/Button";
import Input from "./UI/Input/Input";

const Form = (props) => {
    return (
        <form>
            <Input/>
            <Input/>
            <Button/>
        </form>
    )
}

export default Form;